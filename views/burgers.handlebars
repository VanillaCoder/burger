<div class="container">
  <div class="row">
    {{!-- Fresh Burgers --}}
    <div class="col-md-4">
      <ul>
        {{#each results}}
        {{#unless eaten}}
        <li>
          <p>Burger Type: {{type}}</p>
          <p></p>
          <button class="btn btn-primary burgerBtn" id="{{id}}">Eat me!</button>

        </li>
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col-md-4"></div>
    {{!-- Eaten Burgers --}}
    <div class="col-md-4">
      <ul id="eatenBurgers">
        {{#each results}}
        {{#if eaten}}
        <li>
          <p>Burger Type: {{type}}</p>
          <p>YOU ATE ME</p>
        </li>
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>